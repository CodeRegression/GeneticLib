<?xml version="1.0" ?>
<class>

    <namespace>NVL_AI</namespace>
    <solution>GeneticLib</solution>
    <name>MutationBase</name>
    <description>Utility: Adds `mutations` to candidate solutions</description>

    <includes>
        <include name="iostream" namespace="std" local="false" />
        <include name="../Model/CandidateBase.h" local="true" />
    </includes>

    <constructors>
        <constructor type="empty" />
    </constructors>

    <methods>

        <!-- The logic to mutate a given solution-->
        <method section="Entry Point" access="public" modifier="virtual" return="void" name="Mutate" description="Mutates an existing solution" inline="true">
            <parameter type="CandidateBase *" name="candidate" description="The candidate that we are mutating" />
        </method>

    </methods>
</class>